- api GET /products
    + ApiResponse.pagination luôn trả về page = 1, 
        mặc dù trong request frontend cũng đã gửi đúng page. Ví dụ Fe request page = 2, 
        nhưng Be luôn trả về pagination.page = 1
    + Trong database đang có 137 dòng product, thì phải có 3 trang. Nhưng Fe bấm hiện trang thứ 3,
        thì số lượng product hiện ra lại là 50 (đáng ra là 37). Nghi ngờ nghiêm trọng data trả về 
        giống nhau với mọi request dù Fe có thay đổi tham số page

- api GET /products có query lọc sản phẩm
    + Các tùy chọn bộ lọc như lọc giá tăng dần, giảm dần, lọc theo category, giá min - max... không hoạt động